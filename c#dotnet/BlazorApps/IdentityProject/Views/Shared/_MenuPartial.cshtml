
    <div class="container-fluid">
        <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Index">IdentityProject</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
            <ul class="navbar-nav flex-grow-1">
                @*if (TempData["MenuMaster"] != null)
                {
                    var menuMaster = (List<MenuModel>)TempData["MenuMaster"];
                    var groupMenu = menuMaster.GroupBy(x => x.MainMenuName).ToList();

                    foreach (var menulist in groupMenu)
                    {
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                @menulist.Key
                            </a>
                            <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                                @foreach (var subMenuList in menulist)
                                {
                                    <a class="dropdown-item" asp-controller="=@subMenuList.ControllerName" asp-action="@subMenuList.ActionName">@subMenuList.SubMenuName</a>
                                }
                            </div>
                        </li>

                    }
                }*@
            </ul>
            <partial name="_LoginPartial" />
        </div>
    </div>
